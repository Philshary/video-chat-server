{
  "name": "peerjs-server",
  "version": "1.0.0",
  "description": "A simple PeerJS signaling server for Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "peer": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}