{
  "name": "peerjs-server-final",
  "version": "1.0.0",
  "description": "Final PeerJS signaling server with CORS enabled.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "peer": "^1.0.0",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
